<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="buttonTheme" parent="@android:style/Widget.Button">
        <item name="android:gravity">center</item>
        <item name="android:textColor">@color/icons</item>
        <item name="android:textSize">@dimen/text_large</item>
        <item name="android:foreground">@drawable/ripple_effect</item>
        <item name="android:focusable">true</item>
        <item name="android:clickable">true</item>
    </style>

    <style name="buttonTheme.Stroke">
        <item name="android:background">@drawable/button_stroke_accent</item>
        <item name="android:textColor">@color/colorAccent</item>
    </style>

    <style name="buttonTheme.FillColor">
        <item name="android:background">@drawable/button_fill_background</item>
        <item name="android:textColor">@color/primary_text</item>
    </style>

    <style name="buttonTheme.Transparent">
        <item name="android:background">@null</item>
        <item name="android:textColor">@color/primary_text</item>
    </style>

    <style name="buttonTheme.Flat" parent="buttonTheme">
        <item name="android:background">@color/colorAccent</item>
        <item name="android:textColor">#fff</item>

    </style>

</resources>